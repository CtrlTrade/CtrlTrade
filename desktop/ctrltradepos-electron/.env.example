# API server URL baked into the Expo web bundle at build time.
# Copy this file to .env and set the value before running build:web.
#
# Use your production domain (no trailing slash).
# Example: https://api.yourdomain.com
EXPO_PUBLIC_DOMAIN=api.example.com
